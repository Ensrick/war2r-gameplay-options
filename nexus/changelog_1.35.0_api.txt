The computer-AI features moved to their own mod, AI Fixes (winmm.dll, settings in ai_fixes.toml): the AI log (log_ai), the fix for the computer losing count of its wood cutters after a savegame load (fix_ai_after_load) and the heal cooldown for the computer's paladins (cooldown_for_computer). Gameplay Options ignores those keys now and says so once in its log. Everything that changes YOUR side stays here.
